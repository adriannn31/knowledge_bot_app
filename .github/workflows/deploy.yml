name: Deploy`non: [push]`njobs:`n  build:`n    runs-on: ubuntu-latest`n    steps:`n      - uses: actions/checkout@v4`n      - uses: subosito/flutter-action@v2`n      - run: flutter build web --release --base-href "/knowledge_bot_app/"`n      - uses: Actions-js/push@master`n        with:`n          branch: gh-pages`n          directory: build/web`n          github_token: ${{ secrets.GITHUB_TOKEN }}
